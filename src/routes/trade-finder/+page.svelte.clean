<script lang="ts">
  import TradeFinder from '$lib/features/trade/TradeFinder.svelte';
  import { page } from '$app/stores';
  
  // Get initial values from URL params if provided
  $: userA = $page.url.searchParams.get('userA') || '';
  $: userB = $page.url.searchParams.get('userB') || '';
</script>

<!-- Thin route wrapper - all logic moved to TradeFinder component -->
<TradeFinder {userA} {userB} />
